# Netlify – statički sajt (bez build koraka)
[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"

# SPA-style: sve rute vode na index.html (ako kasnije dodaš rutiranje)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
